# Guide

- rule 0: keep the interface consistent across every screen and state
- rule 1: keep the interface consistent across every screen and state
- rule 2: keep the interface consistent across every screen and state
- rule 3: keep the interface consistent across every screen and state
- rule 4: keep the interface consistent across every screen and state
- rule 5: keep the interface consistent across every screen and state
- rule 6: keep the interface consistent across every screen and state
- rule 7: keep the interface consistent across every screen and state
- rule 8: keep the interface consistent across every screen and state
- rule 9: keep the interface consistent across every screen and state
- rule 10: keep the interface consistent across every screen and state
- rule 11: keep the interface consistent across every screen and state
- rule 12: keep the interface consistent across every screen and state
- rule 13: keep the interface consistent across every screen and state
- rule 14: keep the interface consistent across every screen and state
- rule 15: keep the interface consistent across every screen and state
- rule 16: keep the interface consistent across every screen and state
- rule 17: keep the interface consistent across every screen and state
- rule 18: keep the interface consistent across every screen and state
- rule 19: keep the interface consistent across every screen and state
- rule 20: keep the interface consistent across every screen and state
- rule 21: keep the interface consistent across every screen and state
- rule 22: keep the interface consistent across every screen and state
- rule 23: keep the interface consistent across every screen and state
- rule 24: keep the interface consistent across every screen and state
- rule 25: keep the interface consistent across every screen and state
- rule 26: keep the interface consistent across every screen and state
- rule 27: keep the interface consistent across every screen and state
- rule 28: keep the interface consistent across every screen and state
- rule 29: keep the interface consistent across every screen and state
- rule 30: keep the interface consistent across every screen and state
- rule 31: keep the interface consistent across every screen and state
- rule 32: keep the interface consistent across every screen and state
- rule 33: keep the interface consistent across every screen and state
- rule 34: keep the interface consistent across every screen and state
- rule 35: keep the interface consistent across every screen and state
- rule 36: keep the interface consistent across every screen and state
- rule 37: keep the interface consistent across every screen and state
- rule 38: keep the interface consistent across every screen and state
- rule 39: keep the interface consistent across every screen and state
