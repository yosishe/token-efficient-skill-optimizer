---
name: no-autoinvoke-skill
description: Fixture manifest that deliberately omits both marker kinds.
disable-model-invocation: true
---

# No auto-invocation fixture

The manifest above never auto-triggers, so its description is a label for
explicit invocation rather than a trigger surface.
